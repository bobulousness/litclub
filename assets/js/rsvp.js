document.getElementById('.yes').addEventListener('click', rsvp('y'));
document.getElementById('.no').addEventListener('click', rsvp('n'));
document.getElementById('.maybe').addEventListener('click', rsvp('m'));
let rsvp = function(r) {
    var id = req.query.id;
    if(r==='y'){

    }
}